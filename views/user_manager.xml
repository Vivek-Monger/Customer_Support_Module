<odoo>
  <data>
    <!-- Form View -->
    <record id="view_user_manager_form" model="ir.ui.view">
      <field name="name">user.manager.form</field>
      <field name="model">user.manager</field>
      <field name="arch" type="xml">
        <form string="User Manager">
          <!-- Smart Button Bar -->
          <header>
            <button name="action_view_projects" 
                    type="object" 
                    class="oe_stat_button" 
                    icon="fa-folder-open"
                    invisible="role != 'customer' or not user_id">
              <field name="project_count" widget="statinfo" string="Projects"/>
            </button>
          </header>
          
          <sheet>
            <group>
              <field name="name"/>
              <field name="email"/>
              <field name="role"/>
              <field name="user_id" readonly="1" invisible="1"/>
            </group>
            
            <!-- Projects Notebook Tab (Only for Customers) -->
            <notebook invisible="role != 'customer'">
              <page string="Projects" name="projects">
                <!-- Message when user_id not created yet -->
                <div class="alert alert-info" invisible="user_id">
                  <strong>Note:</strong> Please save this customer record first, then you can assign projects.
                </div>
                
                <!-- Show assigned projects - NO INLINE TREE -->
                <field name="project_ids" 
                       invisible="not user_id"
                       context="{'default_customer_id': user_id}"/>
                
                <!-- Button to assign existing projects -->
                <div invisible="not user_id" class="mt-3">
                  <button name="%(customer_support_module.action_assign_projects_wizard)d" 
                          type="action" 
                          string="Assign Existing Projects" 
                          class="btn-primary" 
                          context="{'default_customer_id': user_id}"/>
                </div>
              </page>
            </notebook>
          </sheet>
        </form>
      </field>
    </record>

    <!-- List View -->
    <record id="view_user_manager_list" model="ir.ui.view">
      <field name="name">user.manager.list</field>
      <field name="model">user.manager</field>
      <field name="arch" type="xml">
        <list string="User Manager">
          <field name="name"/>
          <field name="email"/>
          <field name="role"/>
          <field name="project_count" string="Projects" invisible="role != 'customer'"/>
          <field name="user_id" optional="hide"/>
        </list>
      </field>
    </record>

    <!-- Action -->
    <record id="action_user_manager" model="ir.actions.act_window">
      <field name="name">User Manager</field>
      <field name="res_model">user.manager</field>
      <field name="view_mode">list,form</field>
      <field name="help" type="html">
        <p class="o_view_nocontent_smiling_face">
          Manage Customers and Support Agents here.
        </p>
      </field>
    </record>

    <!-- Menu -->
    <menuitem id="menu_user_manager_root" 
              name="User Manager" 
              parent="customer_support_module.menu_customer_support_root" 
              sequence="20" 
              action="action_user_manager"
              groups="customer_support_module.group_admin"/>

    <!-- Email Template -->
    <record id="email_template_user_credentials" model="mail.template">
      <field name="name">User Credentials</field>
      <field name="model_id" ref="base.model_res_users"/>
      <field name="subject">Your Login Credentials</field>
      <field name="email_from">${(user.company_id.email or 'noreply@example.com')|safe}</field>
      <field name="auto_delete" eval="True"/>
      <field name="body_html">
        <![CDATA[
        <p>Hello ${object.name},</p>
        <p>Your Odoo account has been created. You can login using the following credentials:</p>
        <ul>
            <li><b>Login:</b> ${ctx.get('login')}</li>
            <li><b>Password:</b> ${ctx.get('password')}</li>
        </ul>
        <p>Please change your password after first login.</p>
        <p>Thanks,</p>
        <p>${user.company_id.name}</p>
        ]]>
      </field>
    </record>
  </data>
</odoo>